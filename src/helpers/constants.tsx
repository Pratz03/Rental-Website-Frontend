export enum InputType {
    INPUT = "INPUT",
    SELECT = "SELECT",
}

export enum BooleanType {
    TRUE = "TRUE",
    FALSE = "FALSE"
}

export enum DataType {
    STRING = "TEXT",
    NUMBER = "INTEGER"
}