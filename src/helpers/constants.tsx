export enum InputType {
    INPUT = "INPUT",
    SELECT = "SELECT",
    DATE = "DATE",
    TEXTAREA = "TEXTAREA",
    FILE = "FILE"
}

export enum BooleanType {
    TRUE = "TRUE",
    FALSE = "FALSE"
}